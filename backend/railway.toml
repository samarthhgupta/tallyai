[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 300 --timeout-graceful-shutdown 300"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
